# Interwined

## Development

Install dependencies before running tests in CI or locally:

```bash
poetry install
```

Run the unit tests:

```bash
poetry run python -m unittest discover -s tests/unittests
```
